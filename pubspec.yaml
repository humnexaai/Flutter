name: tasveer_ai
description: High-performance offline passport photo app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  camera: ^0.11.0+1
  image_picker: ^1.1.2
  google_mlkit_face_detection: ^0.12.0
  tflite_flutter: ^0.11.0
  flutter_image_compress: ^2.3.0
  image: ^4.2.0
  pdf: ^3.11.1
  printing: ^5.13.4
  path_provider: ^2.1.4
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
